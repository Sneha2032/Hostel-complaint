<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mess Payment</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/student.css">
</head>

<body>

<!-- HEADER (Same as dashboard) -->
<header class="header">
    <div class="header-content">
        <div class="logo-section">
            <h1 class="logo">🏠 Hostel Portal</h1>
        </div>

        <nav class="menu">
            <a href="/mess" class="nav-link">
                <span class="nav-icon">💳</span>
                <span>Mess Dues</span>
            </a>
            <a href="/application" class="nav-link">
                <span class="nav-icon">📝</span>
                <span>Application</span>
            </a>
            <a href="/complaint" class="nav-link">
                <span class="nav-icon">➕</span>
                <span>New Complaint</span>
            </a>
        </nav>

        <div class="header-right">
            <a href="/mess" class="back-btn" title="Back to Mess Dues">
                <span>← Back to Dues</span>
            </a>
            <a href="/logout" class="logout-btn" title="Logout">
                <span>🚪</span>
            </a>
        </div>
    </div>

</header>

<!-- CONTENT -->
<main class="dashboard-container">
    <div class="content-section">
        <div class="section-header">
            <h2 class="section-title">
                <span class="title-icon">💳</span>
                Confirm Mess Payment
            </h2>
        </div>

        <div class="payment-card">
            <div class="payment-details">
                <div class="payment-row">
                    <span class="label">Date</span>
                    <span class="value"><%= new Date(due.createdAt || Date.now()).toLocaleDateString() %></span>
                </div>
                <div class="payment-row">
                    <span class="label">Semester</span>
                    <span class="value"><%="Current Semester" %></span>
                </div>
                <div class="payment-row amount-row">
                    <span class="label">Amount</span>
                    <span class="value amount">₹<%= due.amount %></span>
                </div>
            </div>

            <!-- Backend unchanged: POST /payment/:id -->
            <form method="POST" action="/payment/<%= due._id %>" class="payment-form">
                <button type="submit" class="btn-primary">
                    Confirm Payment
                </button>
            </form>
        </div>
    </div>
</main>

</body>
</html>
